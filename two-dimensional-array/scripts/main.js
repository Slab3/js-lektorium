function twoDimensArr(columnAmount, rowAmount, columnStart, rowStart, direction = 'left') {

  const totalCellAmount = columnAmount * rowAmount;
  let counter = 1;

  // const matrix = Array.from({length: totalCellAmount}, () => Array(totalCellAmount).fill(counter));

  // return matrix;
}

console.log(twoDimensArr(5,6));

